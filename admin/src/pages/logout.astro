---
// Log out by clearing cookie session
const headers = new Headers();
headers.set('Location', '/admin/login');
headers.set('Set-Cookie', 'queria_session=; HttpOnly; SameSite=Lax; Path=/; Max-Age=0');
return new Response(null, {
  status: 302,
  headers,
});
---
